# Cursor AI Rules for {{projectName}}

AI 개발 환경을 위한 Cursor 에디터 규칙 템플릿입니다.

## 📁 구조

```
.cursor/
└── rules/
    ├── generate-test-code.md     # 테스트 코드 생성 규칙
    ├── refactor-logic.md         # 코드 리팩토링 규칙  
    └── custom-rule-example.md    # 커스텀 규칙 예시
```

## 🎯 사용법

### 1. Cursor 에디터에서 활용
- Cursor 에디터에서 프로젝트를 열면 자동으로 규칙이 적용됩니다
- AI 코드 생성 시 이 규칙들이 참조됩니다
- 각 규칙 파일은 특정 상황에서 AI의 동작을 가이드합니다

### 2. 규칙 커스터마이징
- `custom-rule-example.md`를 참고하여 프로젝트별 규칙을 추가하세요
- 기존 규칙들을 {{projectName}}에 맞게 수정하세요
- {{mainLanguage}} 관련 베스트 프랙티스를 추가하세요

### 3. AI 코드 생성 최적화
- **테스트 코드**: `generate-test-code.md` 규칙에 따라 체계적인 테스트 생성
- **리팩토링**: `refactor-logic.md` 규칙으로 클린 코드 원칙 적용
- **커스텀 로직**: 프로젝트 특화 규칙으로 일관된 코드 스타일 유지

## 🔧 설정 변수

이 템플릿에는 다음 변수들이 적용되었습니다:
- **Project**: {{projectName}}
- **Language**: {{mainLanguage}}
- **Purpose**: {{taskDescription}}

## 💡 활용 팁

### Cursor에서 AI 규칙 사용하기
1. **Ctrl/Cmd + L**: AI 채팅에서 규칙 기반 질문
2. **Tab**: 코드 제안 시 규칙이 자동 적용
3. **Alt + Enter**: 코드 액션에서 규칙 기반 리팩토링

### 효과적인 프롬프트 예시
```
"이 함수에 대한 테스트 코드를 generate-test-code.md 규칙에 따라 생성해줘"
"refactor-logic.md 규칙을 적용하여 이 코드를 리팩토링해줘"
"custom-rule-example.md의 {{projectName}} 컨벤션에 맞게 코드를 수정해줘"
```

## 🚀 고급 활용

### 팀 컨벤션 통합
1. 팀의 코딩 스타일을 `custom-rule-example.md`에 문서화
2. 프로젝트별 아키텍처 패턴을 규칙에 포함
3. 도메인 특화 베스트 프랙티스 추가

### CI/CD 통합
- 생성된 코드가 규칙을 준수하는지 자동 검증
- 린트 규칙과 AI 규칙의 일관성 유지
- 코드 리뷰에서 AI 규칙 준수 여부 확인

---

**Generated by Project Starter CLI**  
**Purpose**: {{taskDescription}}  
**Language**: {{mainLanguage}} 