# Custom AI Rules for {{projectName}}

## Context
Project: {{projectName}}  
Main Language: {{mainLanguage}}  
Purpose: {{taskDescription}}

## Custom Development Rules

This file contains project-specific rules that you can customize for your {{projectName}} development workflow.

### 1. Project-Specific Conventions

#### File Organization:
- Use kebab-case for file names: `user-service.{{mainLanguage}}`
- Group related files in feature directories
- Keep utility functions in dedicated `utils/` folder
- Separate types/interfaces in `types/` directory

#### Naming Conventions:
- **Variables**: camelCase (`userName`, `apiResponse`)
- **Constants**: UPPER_SNAKE_CASE (`API_ENDPOINT`, `MAX_RETRY_COUNT`)
- **Functions**: camelCase with verb prefix (`getUserData`, `validateInput`)
- **Classes**: PascalCase (`UserService`, `PaymentProcessor`)

### 2. Code Documentation Standards

#### Function Documentation:
```{{mainLanguage}}
/**
 * Brief description of what the function does
 * 
 * @param {type} paramName - Description of parameter
 * @returns {type} Description of return value
 * @throws {ErrorType} When error condition occurs
 * 
 * @example
 * const result = functionName(exampleInput);
 */
```

#### Class Documentation:
```{{mainLanguage}}
/**
 * {{projectName}} - [ClassName]
 * 
 * Purpose: {{taskDescription}}
 * 
 * @class ClassName
 * @version 1.0.0
 * @author Generated by Project Starter CLI
 */
```

### 3. Error Handling Patterns

#### For {{mainLanguage}} Projects:
- Always use try-catch for async operations
- Create custom error types for different scenarios
- Log errors with context and stack traces
- Return meaningful error messages to users

#### Example Error Pattern:
```{{mainLanguage}}
try {
  const result = await riskyOperation();
  return { success: true, data: result };
} catch (error) {
  logger.error('Operation failed in {{projectName}}:', {
    error: error.message,
    stack: error.stack,
    context: { /* relevant context */ }
  });
  
  return { 
    success: false, 
    error: 'User-friendly error message' 
  };
}
```

### 4. Testing Strategies

#### Test File Organization:
- Mirror source directory structure in tests
- Use `.test.{{mainLanguage}}` or `.spec.{{mainLanguage}}` extensions
- Group tests by feature or component
- Include integration tests for critical paths

#### Mock Strategy:
- Mock external dependencies (APIs, databases)
- Use factories for test data generation
- Keep mocks simple and focused
- Reset mocks between tests

### 5. Performance Guidelines

#### Optimization Priorities:
1. **Correctness**: Code must work correctly first
2. **Readability**: Code should be easy to understand
3. **Maintainability**: Code should be easy to modify
4. **Performance**: Optimize only when necessary

#### Common Performance Patterns:
- Cache expensive computations
- Use pagination for large data sets
- Implement proper indexing for database queries
- Minimize network requests

### 6. AI Assistance Prompts

#### Custom Prompts for {{projectName}}:
- "Generate {{mainLanguage}} code following {{projectName}} conventions"
- "Create unit tests for {{projectName}} using our testing patterns"
- "Refactor this code to match {{projectName}} style guidelines"
- "Add comprehensive documentation for this {{projectName}} module"
- "Optimize this {{projectName}} function for better performance"

### 7. Code Review Checklist

#### Before Submitting Code:
- [ ] Follows {{projectName}} naming conventions
- [ ] Includes appropriate error handling
- [ ] Has sufficient test coverage
- [ ] Documentation is up to date
- [ ] No console.log statements in production code
- [ ] Performance implications considered

#### Security Considerations:
- [ ] Input validation implemented
- [ ] No sensitive data in logs
- [ ] Authentication/authorization checked
- [ ] SQL injection prevention applied
- [ ] XSS protection implemented

### 8. Customization Instructions

To customize these rules for your specific needs:

1. **Add Project-Specific Patterns**: Include patterns specific to your domain
2. **Update Technology Stack**: Modify examples for your chosen frameworks
3. **Add Team Conventions**: Include team-specific coding standards
4. **Include Business Logic**: Add domain-specific validation rules

### 9. Integration with Development Tools

#### IDE Configuration:
- Configure linting rules to match these conventions
- Set up auto-formatting to apply style guidelines
- Create code snippets for common patterns
- Configure debugging tools for error tracking

#### CI/CD Integration:
- Run linting and formatting checks in pipeline
- Enforce test coverage thresholds
- Include security scanning
- Automate documentation generation

---
*Custom rules for {{projectName}} - {{taskDescription}}*

**Note**: This is a template file. Customize it according to your specific project requirements and team preferences. 